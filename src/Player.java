/**
 * An enum class storing all possible players, including X, O, None, and Both. 
 * None and Both are never assigned to actual players and only used for internal 
 * purposes
 */
public enum Player {
    X, O, None, Both
}
